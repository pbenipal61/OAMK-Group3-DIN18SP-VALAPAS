<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.group3.valapas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignUpActivity"></activity>
        <activity android:name=".SignInActivity" />
        <activity android:name=".BrowseActivity" />
        <activity android:name=".Activities.BrowsePage" />
        <activity android:name=".Activities.Customers.CustomerBookingsActivity" />
        <activity android:name=".Activities.Customers.CustomerFavoritesActivity" />
        <activity android:name=".Activities.Customers.CustomerProfileActivity" />
        <activity android:name=".Activities.Customers.UserBrowseTab" />
        <activity android:name=".Activities.Customers.CustomerRegisterActivity" />
        <activity android:name=".Activities.Companies.CompanyRegisterActivity" />
        <activity android:name=".Activities.RegisterTypeActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>